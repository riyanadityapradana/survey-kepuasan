// assets/script.js - JS untuk interaksi tambahan
// Contoh: Fokus input login otomatis, animasi tombol, dll

document.addEventListener("DOMContentLoaded", function () {
  var input = document.querySelector("input[autofocus]");
  if (input) input.focus();
  // Tambahkan interaksi lain sesuai kebutuhan
});
